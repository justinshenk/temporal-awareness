setup:
  model: "Qwen/Qwen3-4B-Instruct-2507"
  seed: 0
  batch_size: 32
  dtype: "bfloat16"
  granularity: "fine"

paths:
  data_loc: "./data/raw/temporal_scope"
  save_loc: "./results/eap_integrated_gradients/intertemporal_50_unconstarined"

input:
  data_file: "intertemporal.json"
  template: "{}\n\n{}\n{}"
  option_keys: [ "(A)", "(B)" ]
  prompt_suffix: "Answer: ("
  horizon: [ "ST", "LT" ]

output:
  wandb_project: "model_exploration"
  filename: "intertemporal_50_unconstarined"

parameters:
  system_prompt: "You are evaluating a preference between two options.

    Respond in the following format:

    'Answer: (A)' if you prefer option A

    'Answer: (B)' if you prefer option B"
  steps: [ 20 ]
  metric_type: "option-logit"
