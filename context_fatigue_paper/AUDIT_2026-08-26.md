# Full-paper audit vs codebase and artifacts — 2026-08-26

Six parallel verification passes traced every quantitative and methodological claim in
`context_fatigue.tex` through `numbers.md` → run notes → committed `summary.json`/`turns.csv`
artifacts → driver code. Where committed raw data exists, values were **recomputed
independently** (window-split closure item-for-item, competition closure panel, calibration
block, Fig. 5 attention shares, per-head restoration bootstrap, all Qwen replication numbers,
every ΔΔ cell). Everything recomputable reproduced. The findings below were fixed in the tex
and `numbers.md` the same day; provenance gaps remain open.

## Fixed in tex (substantive)

1. **§4.3 "2.0% (6.5% at CI bound)" withdrawn.** The figure converted the layer-24-only mass
   delta through the E1f dose-response slope — the cross-denomination arithmetic
   `E4_HEAD_STRUCTURE.md` explicitly invalidates (the companion 50× figure was withdrawn on
   the same ground), and it contradicted the all-layer 0.0022 quoted in the same sentence.
   Tex now states only the supported share contrast (0.0455 vs 0.0022, ~20×).
2. **§4.1 adherence-null attribution.** The only adherence artifact
   (`instruction_adherence/`) is a Qwen2.5-7B 32k run; the tex presented the null among OLMo
   results and said "both nulls replicate on Qwen." Now attributed to Qwen explicitly; only
   the accuracy null is claimed as replicating.
3. **Appendix J ΔΔ estimand.** Was described as "against the unpatched transcripts"; the
   committed estimand is against a **self-patch baseline**, chosen because preflight showed
   the unpatched baseline confounds capture-openness with donor identity. Corrected, with the
   rationale, plus: prefix-only readout caveat (no canonical format in any of 90 graded
   replies), exact mirror-image degeneracy of the two directions on Qwen, and the sign
   inversion (localization claim is direction-independent). Fixed earlier the same day.
4. **Appendix J "all-layer pooled share readout"** was false for the distance sweep
   (layer-24-indexed); the +10.4 causal vs +10.7 observational slope match compares different
   share denominators — both now stated.
5. **Appendix J "unrelated control ≤|0.11| in every quoted cell"** violated by last_2
   (+0.264) and template_rand (−0.113); exceptions now stated (both cells' counterfactual
   effects are near null). Control renamed unrelated-**fact** patch (same instruction, one
   clause changed) to match the driver.

## Fixed in tex (minor)

- §4.2: dose-response paragraph and Fig. 2b caption now signal the L24 vs all-layer
  denomination (endpoint agreement 0.004 L24 / 0.017 all-layer); evidence span "~90 tokens" →
  "~280" (artifact mean 279); Table 1 caption's capture claim left as-is (determinism-checked
  adjacent forward).
- §4.3: "0.42 of the context body's mass on delimiters" relabeled — 0.416 is the top-≈128
  most-read tokens' share of the *entire* final-position row (delimiter-only ≈0.62 of body,
  understated before); §4.5 cross-reference matched.
- §4.4: code arm "0.90 at fill 0.78" → "1.00 … at fill 0.78, the deepest interpreted point"
  (aligned to numbers.md; artifact missing — see gaps); enrichment values marked "at peak".
- §4.6: "confidently-wrong gap widens ~10 points" → wrong-answer confidence rises ~10 points
  (the literal gap widens ~3).
- §4.1: correlations scoped "over fill <0.8" per `NULL_STATISTICS.md`; 56 → 57 MMLU subjects.
- §2.2: "~10×" system-share fall → "roughly an order of magnitude" (E6 source uncommitted;
  committed E5 value is 8×). §1: "drops roughly 3–4×" (Llama forward is 2.8×).
- Appendix B: "answers 'A' 110/110" → "103–110 of 110" (−4.72-nat level is 103/110).
- Appendix C: canary count split (system-clamp arms three-part; adherence arm prefix+suffix).
- Appendix E: "all intervals at 10,000 draws" → joint-fit/slope intervals at 4,000.
- Appendix H: filler-question closure 0.132 now paired with the committed re-run's 0.100.
- Appendix J: n bookkeeping (full patch n=39 stage-1, bisection n=23, OLMo glue n=85/full
  n=34, code erosion endpoint n=17); random-context window is 4k; OLMo attention null sign
  orientation (−0.0003 → +0.0003 matched); Fig. 9 caption suffix bracket 0.12–0.09.
- Table 4 caption: n=24 → n=39/23.

## Fixed in numbers.md

Rows 88 (2.0%/6.5% withdrawn), 242 (orientation), 268 (hot-set relabel), 286 (effective n),
292 (control-bound exceptions).

## Open provenance gaps (NOT fixable in the tex)

1. **The OLMo E6 evidentiary base is uncommitted.** `E6_FORMAT_EROSION.md` and the run dirs
   (`e6_code/`, `e6_gsm8k/`, `e6_mmlu/`, `e6_*_spans/`, `e6_mmlu_recovery/`,
   `e6_exemplar_close/`, `e6_format_probes/` incl. captures, `e6_mode_steering*`,
   `e6_probe_dir_erase_*`) exist nowhere in the repo or git history, though numbers.md and
   `paper_figures.py` cite them. Affected headline claims: the erosion ladder, reversal
   accuracies, enrichment ordering, probe AUCs, steering/erase results, §2.2's order-of-
   magnitude curve, Fig. 3's data, and Appendix E's skip counts (2 mmlu / 32 code). The
   rank≈2 claim additionally has **no committed analysis code**. → Recover from the A100 box
   and commit, or re-run.
2. **Original OLMo competition/head artifacts gone**: `e3_competition/`, `e3_attention/`,
   `e3c_competitor_close/`, `e1_heads_all/`, `e3_heads_all/`. The disjoint-arm accuracy,
   joint fit, committed closure row, cross-family DiD interval, and all Appendix F per-head
   numbers rest on run notes alone; Fig. 2c's data path no longer resolves.
3. **Adherence run**: prose note only, no summary.json/turns.csv committed.
4. **Pooled random-context CSV** (`random_context_topbin/turns_pooled.csv`) behind the
   n=699/1001 nulls is absent; only the smaller n=344/483 stream is committed.
5. **Smaller**: E5 raw dirs uncommitted (note only); Appendix B's 1.5e-8 / 0.0 capture
   validation traces only to a commit message (tests assert 1e-5 on tiny models); "48-token
   system prompt" has no recorded token count; Qwen seeds asserted in notes, not recorded in
   summaries; two Qwen mmlu runs disagree on depth-42 natural compliance (0.0333 vs 0.0667,
   different generation passes); glue-subset periodicity control still open (already in
   Limitations).
