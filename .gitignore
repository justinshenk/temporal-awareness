# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.egg

# Virtual environments
venv/
.venv/
env/
tam_env/

# IDE
.idea/
.vscode/
*.swp
*.sublime-project
*.sublime-workspace

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Large model/data files
*.npy
*.npz
*.pt
*.bin

# Keep checkpoints (pkl probes, json vectors)
!results/checkpoints/*.pkl
!results/checkpoints/*.json

# Secrets
.env

# SLURM / experiment logs (regenerated per run)
logs/

# W&B local cache
wandb/

# Results and data (large, tracked via W&B)
results/
data/

# Word docs (personal notes)
*.docx

# LaTeX artifacts
texput.log

# Experiments
out/

# Other
tmp/
temp/
git_worktrees/
worktrees/
worktree/
wrktr/
paper/

# Local-only working trees and snapshots (never delete, never commit)
rebuttal_RESCUED_from_trash/
old_paper_tree_snapshot.tar.gz

# Data re-downloaded from the HF dataset (25 GB; never re-upload, never commit)
cloud/pulled/

# Per-box fleet state (instance ids, run env; regenerated per campaign)
cloud/.run_*
cloud/.instances_*
cloud/.fleet_audit.log
.instances_ours
.instances_foreign

# Claude Code local session artifacts
.claude/

